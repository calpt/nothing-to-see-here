# Adapter-Hub adapter entry
# Defines a single adapter entry in Adapter-Hub
# --------------------

# The name of the author(s) of this adapter.
author: "Andreas Rücklé"

# A bibtex citation of the work related to this adapter.
citation: 

# The string identifier of the adapter architecture (must be available in architecture).
# Describes the adapter architecture used by this adapter
config:
  # The name of the adapter config used by this adapter (a short name available in the `architectures` folder).
  # Example: pfeiffer
  using: pfeiffer

# The version to be downloaded if no version is explicitly stated.
default_version: "1"

# A short description of this adapter.
description: |
  STS-B adapter (with head) trained using the `run_glue.py` script with an extension for early stopping.

# A contact email of the author(s).
email: "rueckle@ukp.informatik.tu-darmstadt"

# A list of different versions of this adapter available for download.
files:
  - sha1: "21160010DE74E26E6AEE2F2C95B53CB033B48CB8"
    sha256: "4886A3D0B0C2457124DF2B12DE249A57F33370691ECB3947242559F696C163E2"
    # Download URL pointing to a zip folder containing the adapter module.
    url: "https://public.ukp.informatik.tu-darmstadt.de/AdapterHub/text_task/sts-b/roberta-base/houlsby/roberta-base_sts-b_houlsby.zip"
    version: "1"
    description: Achieves 90.39 Spearman rank correlation on the STS-Benchmark (devset)


# A GitHub handle associated with the author(s).
github: "arueckle"

# The hidden size of the model
hidden_size: 768

# The string identifier of the pre-trained model (by which it is identified at Huggingface).
# Example: bert-base-uncased
model_name: "roberta-base"

# The model type.
# Example: bert
model_type: roberta

# The string identifier of the subtask this adapter belongs to.
subtask: sts-b

# The string identifier of the task this adapter belongs to.
task: sts

# A Twitter handle associated with the author(s).
twitter: "@arueckle"

# The type of adapter (one of the options available in `adapter_type`.
type: "text_task"

# A URL providing more information on this adapter/ the authors/ the organization.
url: "http://rueckle.net"